import TextField from "@mui/material/TextField";

const TextPassword = () => {
  return (
    <TextField
      id="filled-password-input"
      label="Password"
      type="password"
      variant="filled"
    />
  );
};

export default TextPassword;
